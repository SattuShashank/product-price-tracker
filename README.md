## Run

```bash
python manage.py migrate
```
```bash
python manage.py import_kaggle_prices ../data/data.csv
```
```bash
python manage.py runserver
```

In another terminal window (to scrap data) Run:  -> Still Experimenting: Might not work.

```bash
python manage.py scrape_marketplaces
```
